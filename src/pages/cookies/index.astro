---
import Layout from '../../layouts/Layout.astro';
import MetaHead from '../../components/MetaHead.astro';
const brand = 'LatAm Scalers';

const meta = {
	title: `Cookie Policy | ${brand}`,
	description:
		'Cookie policy explaining how LatAm Scalers uses cookies and similar technologies, and how you can manage preferences.',
};

const baseUrl = Astro.site ?? 'https://latam-scalers-website.vercel.app';
const canonical = new URL('/cookies/', baseUrl).toString();
---

<Layout>
	<MetaHead slot="head" title={meta.title} description={meta.description} canonical={canonical} />

	<main class="mx-auto max-w-6xl px-6 py-12 sm:py-14">
		<header class="max-w-3xl">
			<p class="text-sm font-semibold tracking-wide text-accent">Legal</p>
			<h1 class="mt-4 text-4xl font-semibold tracking-tight text-text-primary sm:text-5xl">Cookie Policy</h1>
			<p class="mt-4 text-sm leading-relaxed text-text-secondary sm:text-base">
				{/* NOTE: Placeholder content. Replace with your finalized cookie details and vendor list. */}
				Effective date: <span class="text-text-primary">TODO</span>
			</p>
		</header>

		<div class="mt-10 grid gap-4">
		<section class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
			<h2 class="text-lg font-semibold text-text-primary">What cookies are</h2>
				<p class="mt-3 text-sm leading-relaxed text-text-secondary">
					TODO: Define cookies and similar technologies (pixels, local storage).
				</p>
			</section>

		<section class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
			<h2 class="text-lg font-semibold text-text-primary">How we use cookies</h2>
				<ul class="mt-4 grid gap-2 text-sm text-text-secondary">
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">
						<strong class="text-text-primary">Essential</strong>: site functionality and security.
					</li>
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">
						<strong class="text-text-primary">Analytics</strong>: understand usage and improve the site.
					</li>
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">
						<strong class="text-text-primary">Marketing</strong>: measure campaigns (if enabled).
					</li>
				</ul>
			</section>

		<section class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
			<h2 class="text-lg font-semibold text-text-primary">Managing preferences</h2>
				<p class="mt-3 text-sm leading-relaxed text-text-secondary">
					TODO: How users can manage cookies via browser settings and (if implemented) a consent banner.
				</p>
			</section>

		<section class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
			<h2 class="text-lg font-semibold text-text-primary">Cookie list</h2>
				<p class="mt-3 text-sm leading-relaxed text-text-secondary">
					TODO: Add vendor list (name, purpose, duration, type) once finalized.
				</p>
			</section>
		</div>
	</main>
</Layout>


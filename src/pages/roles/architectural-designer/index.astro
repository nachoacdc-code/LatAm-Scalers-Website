---
// ROLE TEMPLATE — copy this file for new role pages and update:
// - meta title/description + canonical path
// - hero headline/subheadline
// - role overview / why remote works / options links / sample outputs / proof / CTAs
import Layout from '../../../layouts/Layout.astro';
import MetaHead from '../../../components/MetaHead.astro';
import ContactSection from '../../../components/ContactSection.astro';
import { BOOKING_URL, isExternalUrl } from '../../../lib/cta';

const brand = 'LatAm Scalers';

const meta = {
	title: `Dedicated Remote Architectural Designer | ${brand}`,
	description:
		'Dedicated remote architectural designers for US firms — conceptual design, Revit modeling, and construction documents. Flexible individuals or team scaling.',
};

const baseUrl = Astro.site ?? 'https://latam-scalers-website.vercel.app';
const canonical = new URL('/roles/architectural-designer/', baseUrl).toString();

const roleName = 'Architectural Designer';

const bookingUrl = BOOKING_URL;
const bookingExternal = isExternalUrl(bookingUrl);
---

<Layout>
	<MetaHead slot="head" title={meta.title} description={meta.description} canonical={canonical} />

	<main class="">
		{/* HERO */}
		<section class="relative overflow-hidden bg-primary">
			<div class="mx-auto max-w-6xl px-6 py-16 sm:py-20">
				<div class="grid items-center gap-10 lg:grid-cols-2">
					<div>
						<p class="text-sm font-semibold tracking-wide text-accent">Roles</p>
						<h1 class="mt-4 text-4xl font-semibold tracking-tight text-white sm:text-5xl">
							Dedicated Remote Architectural Designer
						</h1>
						<p class="mt-4 max-w-prose text-base leading-relaxed text-text-secondary sm:text-lg">
							Vetted experts in conceptual design, schematic development, Revit modeling, and construction documents —
							ready to integrate into your US firm's workflow.
						</p>

						<div class="mt-8 flex flex-wrap gap-3">
							<a
								class="inline-flex items-center justify-center rounded bg-accent px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:"
								href={bookingUrl}
								{...(bookingExternal ? { target: '_blank', rel: 'noreferrer' } : {})}
								data-book-call
							>
								Book a 15-Min Call
							</a>
							<a
								class="inline-flex items-center justify-center rounded-xl border border-white/20 bg-white/5 px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-white/10 focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2"
								href="/#contact"
								data-open-tell-us="role-hero"
								data-role={roleName}
							>
								Tell Us Your Needs
							</a>
						</div>
					</div>

					<div class="relative">
						<div class="rounded-2xl border border-white/10 bg-white/5 backdrop-blur-sm p-6 shadow-sm">
							<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">At a glance</p>
							<ul class="mt-4 grid gap-3 sm:grid-cols-2">
								<li class="rounded-xl border border-white/10 bg-white/5 p-4">
									<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Tools</p>
									<p class="mt-2 text-sm text-text-secondary">Revit, AutoCAD, SketchUp, Lumion</p>
								</li>
								<li class="rounded-xl border border-white/10 bg-white/5 p-4">
									<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Standards</p>
									<p class="mt-2 text-sm text-text-secondary">AIA workflows, IBC familiarity</p>
								</li>
								<li class="rounded-xl border border-white/10 bg-white/5 p-4">
									<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Delivery</p>
									<p class="mt-2 text-sm text-text-secondary">Fast redlines + documentation</p>
								</li>
								<li class="rounded-xl border border-white/10 bg-white/5 p-4">
									<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Flex</p>
									<p class="mt-2 text-sm text-text-secondary">20h/40h or team scaling</p>
								</li>
							</ul>
						</div>
					</div>
				</div>
			</div>
		</section>

		<div class="mx-auto max-w-6xl px-6 py-12">
			{/* ROLE OVERVIEW */}
			<section class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
				<h2 class="text-xl font-semibold text-text-primary">Role overview</h2>
				<p class="mt-3 text-sm leading-relaxed text-text-secondary sm:text-base">
					Our architectural designers bring firm-level expertise at 50%+ lower cost. Timezone-aligned, fluent English,
					and pre-vetted for tools like Revit, AutoCAD, SketchUp, and Lumion.
				</p>
				<ul class="mt-5 grid gap-2 text-sm text-text-secondary sm:grid-cols-2">
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">Schematic and design development</li>
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">3D modeling and rendering</li>
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">Construction documentation</li>
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">Code compliance support</li>
					<li class="rounded-xl border border-border bg-bg-alt px-4 py-3">Coordination with engineers and consultants</li>
				</ul>
			</section>

			{/* WHY REMOTE WORKS */}
			<section class="mt-12">
				<h2 class="text-xl font-semibold text-text-primary">Why remote architectural designers excel</h2>
				<ul class="mt-4 grid gap-2 text-sm text-text-secondary sm:grid-cols-2">
					<li class="rounded-xl border border-border bg-bg-alt/40 shadow-sm px-4 py-3">Handle peak workloads without delays</li>
					<li class="rounded-xl border border-border bg-bg-alt/40 shadow-sm px-4 py-3">Seamless redline integration and iteration</li>
					<li class="rounded-xl border border-border bg-bg-alt/40 shadow-sm px-4 py-3">Proven experience with US firm standards (AIA, IBC)</li>
					<li class="rounded-xl border border-border bg-bg-alt/40 shadow-sm px-4 py-3">Fast turnaround on deliverables</li>
				</ul>
			</section>

			{/* OPTIONS */}
			<section class="mt-12" aria-labelledby="flexible-options">
				<h2 id="flexible-options" class="text-xl font-semibold text-text-primary">
					Options for this role
				</h2>
				<div class="mt-6 grid gap-4 md:grid-cols-2">
					<article class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
						<h3 class="text-base font-semibold text-text-primary">Individual Specialist</h3>
						<p class="mt-3 text-sm leading-relaxed text-text-secondary">
							Dedicated full-time (40h/week) or part-time (20h/week) architectural designer embedded in your team.
						</p>
						<a
							href="/solutions/aec-talent/individuals/"
							class="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-accent transition hover:text-accent/80 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2"
						>
							View individuals <span aria-hidden="true">→</span>
						</a>
					</article>

					<article class="rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
						<h3 class="text-base font-semibold text-text-primary">Team Integration</h3>
						<p class="mt-3 text-sm leading-relaxed text-text-secondary">
							Add architectural designers to a custom production pod with drafters, engineers, and optional PM
							coordination.
						</p>
						<a
							href="/solutions/aec-talent/teams/"
							class="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-accent transition hover:text-accent/80 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2"
						>
							View teams <span aria-hidden="true">→</span>
						</a>
					</article>
				</div>
			</section>

			{/* SAMPLE OUTPUTS (anonymized) */}
			<section class="mt-12" aria-labelledby="sample-outputs">
				<h2 id="sample-outputs" class="text-xl font-semibold text-text-primary">Sample outputs (anonymized)</h2>
				<div class="mt-6 grid gap-4 md:grid-cols-3">
					<div class="aspect-[4/3] rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-4">
						<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Gallery</p>
						<p class="mt-2 text-sm text-text-secondary">TODO: Schematics placeholder</p>
					</div>
					<div class="aspect-[4/3] rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-4">
						<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Gallery</p>
						<p class="mt-2 text-sm text-text-secondary">TODO: Renders placeholder</p>
					</div>
					<div class="aspect-[4/3] rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-4">
						<p class="text-xs font-semibold uppercase tracking-wide text-text-secondary">Gallery</p>
						<p class="mt-2 text-sm text-text-secondary">TODO: Construction documents placeholder</p>
					</div>
				</div>
			</section>

			{/* PROOF */}
			<section class="mt-12" aria-labelledby="proof-role">
				<h2 id="proof-role" class="text-xl font-semibold text-text-primary">Proof</h2>
				<p class="mt-3 max-w-prose text-sm leading-relaxed text-text-secondary">
					{/* TODO: Replace with testimonials carousel. */}
					Testimonials placeholder (to be filled).
				</p>
				<figure class="mt-6 rounded-2xl border border-border bg-bg-alt/40 shadow-sm p-6">
					<blockquote class="text-sm leading-relaxed text-text-secondary italic">
						"Added two architectural designers — cut concept phase time by 40%."
					</blockquote>
					<figcaption class="mt-3 text-xs text-text-secondary">— Anonymized client</figcaption>
				</figure>
			</section>

			{/* CTAs */}
			<section class="mt-14 rounded-2xl border border-border bg-primary p-6 sm:p-8">
				<div class="flex flex-col items-start justify-between gap-6 md:flex-row md:items-center">
					<div>
						<h2 class="text-2xl font-semibold tracking-tight text-white sm:text-3xl">Ready to add architectural design capacity?</h2>
						<p class="mt-2 max-w-prose text-sm leading-relaxed text-text-secondary sm:text-base">
							Book a call to discuss your needs — we'll recommend the best engagement model and match options fast.
						</p>
					</div>
					<div class="flex flex-wrap gap-3">
						<a
							href={bookingUrl}
							{...(bookingExternal ? { target: '_blank', rel: 'noreferrer' } : {})}
							class="inline-flex items-center justify-center rounded bg-accent px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-accent focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:"
							data-book-call
						>
							Book a 15-Min Call
						</a>
						<a
							href="/solutions/aec-talent/"
							class="inline-flex items-center justify-center rounded-xl border border-white/20 bg-white/5 px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-white/10 focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2"
						>
							View AEC solution
						</a>
					</div>
				</div>
			</section>
		</div>
	</main>

	{/* Keep consistent global contact section available (optional) */}
	<div class="mx-auto max-w-6xl px-6">
		<ContactSection subject={`${brand} — new inquiry (role: ${roleName})`} />
	</div>
</Layout>
